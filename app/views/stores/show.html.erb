<h1>Store Blah blah</h1>


    <% @items.each do |item| %>
      <tr>
        <td><%= item.name %></td>
        <td><%= item.description %></td>

        <td><%= image_tag(item.image.url, :width => 100) %></td>

      </tr>
    <% end %>